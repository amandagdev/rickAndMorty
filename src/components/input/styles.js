import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    height: 40,
    backgroundColor: '#fff',
    paddingLeft: 20,
    borderRadius: 20,
    fontSize: 18,
  },
});

export default styles;
